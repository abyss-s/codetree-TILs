#include <iostream>
using namespace std;


int main() {
  // 실행속도 높이기
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  string str;
  cin >> str;
  cout << str << "Hello";

  return 0;
}